# Covex Voice (Vite + React + Tailwind)

This is a zero-config project ready for Vercel.

## Scripts
- `npm run dev` — local dev
- `npm run build` — production build (Vercel runs this automatically)

